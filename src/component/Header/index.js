import { Layout } from "antd";
import "./style.css";
const { Header } = Layout;
export function Headers() {
  return (
    <Header
      className="site-layout-background"
      style={{
        padding: 0,
      }}
    />
  );
}
